Unlike the first two, crossword handles Constraint Satisfaction Problems (CSPs). Instead of working with random probabilities, your AI treats a blank crossword grid as a puzzle with strict mathematical constraints (e.g., words must fit the exact number of squares, and intersecting letters must match perfectly). You implement node consistency, arc consistency (using the AC-3 algorithm), and backtracking search to find the single best way to complete the grid.
